# Shared appearance settings referenced across program modules.
# Passed to every home-manager module via `_module.args.theme` (see home.nix).
{
  # Monospace font used by terminals and editors.
  fontFamily = "SF Mono";
}
